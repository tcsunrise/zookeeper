# ZooKeeper 本地开发指南

> 分支：`dev-maven-epoch`（ZooKeeper 3.4.14 代码，Ant + Maven 双构建，已模块化：
> `zookeeper-jute` / `zookeeper-server` / `zookeeper-client` / `zookeeper-recipes` / `zookeeper-docs`）
> 运行/编译统一使用 **JDK 8**（`C:\Program Files\Java\jdk1.8.0_503`）。

---

## ⚠️ 关键前提：为什么必须用 JDK 8

本仓库对 JDK 版本敏感，根因是 **Maven 的 compiler 插件即使在 JDK8 下运行，也会 fork 到 PATH 上的 JDK25 javac**
（PATH 含 `jdk-25.0.3\bin` 和 Oracle javapath）。这会踩两个坑：

1. **`Record` 歧义**：jute 生成代码 `implements Record` 与 JDK16+ 引入的 `java.lang.Record`
   经 `import org.apache.jute.*` 通配符冲突 → `error: reference to Record is ambiguous`（200 处）。
2. **`ByteBuffer.clear()` 协变坑**：JDK25 用 `-target 8` 编出「`ByteBuffer.clear()` 返回 `ByteBuffer`」的字节码
   （`-target 8` ≠ `--release 8`），运行在 JDK8 上 →
   `NoSuchMethodError: java.nio.ByteBuffer.clear()Ljava/nio/ByteBuffer;`。
   现象：服务端能启动，但**客户端一操作就 ConnectionLoss**。

> 用**真正的 JDK 8 javac** 编译，两个坑都不存在。`pom.xml` 保持原样（`source/target=1.6`）无需改动。

---

## 一、本地启动方案

启动前先准备好 `conf\zoo.cfg`（已生成，standalone 单机）：

```properties
tickTime=2000
initLimit=10
syncLimit=5
dataDir=D:/data/zookeeper   # 仓库外，避免污染 git；目录需存在
clientPort=2181
```

### 方案 A：脚本启动（需要先铺好 build 布局）

根目录 `bin\zkServer.cmd` 的 classpath 只认 **`build\classes` + `build\lib\*`**（Ant 开发者模式）
或发布包的 `..\*;..\lib\*`，**不认 Maven 的 `target\classes`**。所以先铺布局（见 [第三节](#三本地打包与构建)），再启动。

**⭐ 推荐：用根目录的 cmd 包装脚本（自动把 `JAVA_HOME` 指到 JDK8，双击或 cmd 里都能跑）**

| 脚本 | 作用 |
|------|------|
| `build-local.cmd` | 一键构建：fork JDK8 javac 编译 + 铺好 `build\classes`/`build\lib` |
| `run-server.cmd` | 启动服务端（前台 standalone，2181） |
| `run-cli.cmd` | 启动客户端（默认连 `127.0.0.1:2181`） |

```bat
:: 在 cmd 里，进入项目目录后：
build-local.cmd          :: 首次或改代码后构建
run-server.cmd           :: 启动服务端（此窗口前台运行）
```

> 换 JDK8 路径：先 `set ZK_JDK8=你的\jdk8\路径` 再运行脚本。
> 脚本为纯 ASCII，避免中文注释在 GBK 控制台下乱码。

**或直接用原生 `bin` 脚本：**

| 平台               | 命令                                        | 说明                             |
|------------------|-------------------------------------------|--------------------------------|
| Windows          | `bin\zkServer.cmd`                        | **前台** standalone，监听 2181，关窗即停 |
| Git-bash / Linux | `bin/zkServer.sh start`                   | 后台守护进程                         |
| Git-bash / Linux | `bin/zkServer.sh start-foreground`        | 前台运行                           |
| Git-bash / Linux | `bin/zkServer.sh {stop\|status\|restart}` | 停止 / 状态 / 重启                   |

> Windows 的 `zkServer.cmd` **只有前台一种模式**，没有 start/stop 子命令；它实际执行
> `org.apache.zookeeper.server.quorum.QuorumPeerMain conf\zoo.cfg`。
> 脚本通过 `zkEnv.cmd` 读取 `JAVA_HOME`，**启动前务必把 `JAVA_HOME` 指向 JDK8**。

在 cmd 里手动设 `JAVA_HOME` 后启动：

```bat
set JAVA_HOME=C:\Program Files\Java\jdk1.8.0_503
cd /d D:\workspaces\opensource\zookeeper
bin\zkServer.cmd
```

> 从 Git Bash 里用 `cmd.exe /c "set X && a.cmd"` 调批处理会失败（cmd 被当交互式打开后立即退出）；
> 在真正的 cmd 窗口里上面的写法没问题，或直接用 `run-server.cmd`。

### 方案 B：IDE 直接运行 main（调试最方便）

`.idea\runConfigurations\` 下已配置三个启动项，module 均为 `zookeeper-server`，
且已绑定运行 JDK8（`ALTERNATIVE_JRE_PATH`）：

| 配置名                                            | 主类                                                  | 参数                       |
|------------------------------------------------|-----------------------------------------------------|--------------------------|
| **ZK Server (QuorumPeerMain)**                 | `org.apache.zookeeper.server.quorum.QuorumPeerMain` | `conf/zoo.cfg`           |
| **ZK Server Standalone (ZooKeeperServerMain)** | `org.apache.zookeeper.server.ZooKeeperServerMain`   | `conf/zoo.cfg`           |
| **ZK Client (zkCli)**                          | `org.apache.zookeeper.ZooKeeperMain`                | `-server 127.0.0.1:2181` |

> `QuorumPeerMain`：通用入口，配置里只有一个 server 时自动走单机模式，多个时走集群。
> `ZooKeeperServerMain`：纯单机入口，不含选举逻辑。
> 前提：项目 SDK 已设为 JDK8（`misc.xml` 中 `project-jdk-name="1.8"`），编译运行都统一在 JDK8。

### 方案 C：本地伪集群（一台机器多实例）

准备 `zoo1/2/3.cfg`，各自不同的 `dataDir` 与 `clientPort`（2181/2182/2183），并加：

```properties
initLimit=10
syncLimit=5
server.1=127.0.0.1:2888:3888
server.2=127.0.0.1:2889:3889
server.3=127.0.0.1:2890:3890
```

每个 `dataDir` 下放一个 `myid` 文件（内容分别是 `1/2/3`），再用 `QuorumPeerMain` 分别指向各自配置启动。

---

## 二、客户端如何连接

### 命令行客户端（入口 `org.apache.zookeeper.ZooKeeperMain`）

| 平台               | 命令                                     |
|------------------|----------------------------------------|
| Windows          | `bin\zkCli.cmd -server 127.0.0.1:2181` |
| Git-bash / Linux | `bin/zkCli.sh -server 127.0.0.1:2181`  |

- 不加 `-server` 默认连 `localhost:2181`。
- 连集群多地址：`-server host1:2181,host2:2181,host3:2181`。
- 支持单条命令模式（跑完即退）：`bin\zkCli.cmd -server 127.0.0.1:2181 ls /`。

**用包装脚本（cmd，已验证通过）：**

```bat
run-cli.cmd                       :: 交互模式，默认连 127.0.0.1:2181
run-cli.cmd ls /                  :: 单条命令（自动补默认 -server）→ [hello, zookeeper]
run-cli.cmd create /hello world   :: → Created /hello
run-cli.cmd get /hello            :: → world
run-cli.cmd -server 127.0.0.1:2181 ls /   :: 显式指定 server
```

常用命令：`ls /`、`create /path data`、`get /path`、`set /path data`、`delete /path`、`stat /path`、`quit`。

### Java API 连接

```java
ZooKeeper zk = new ZooKeeper("127.0.0.1:2181", 30000, event -> {
    System.out.println("event: " + event);
});
```

---

## 三、本地打包与构建

### Maven（推荐）

模块顺序：`zookeeper-docs → zookeeper-jute → zookeeper-server → zookeeper-client → zookeeper-recipes`。

**⭐ 必须强制 fork 到真实 JDK8 javac：**

```bash
export JAVA_HOME="/c/Program Files/Java/jdk1.8.0_503"

mvn -DskipTests -pl zookeeper-jute,zookeeper-server -am clean install \
  -Dmaven.compiler.fork=true \
  -Dmaven.compiler.executable="C:\Program Files\Java\jdk1.8.0_503\bin\javac"
```

校验字节码正确（应为 `Buffer` 而非 `ByteBuffer`）：

```bash
javap -c org/apache/jute/BinaryOutputArchive.class | grep clear
#   → invokevirtual  // Method java/nio/ByteBuffer.clear:()Ljava/nio/Buffer;   ✅
```

> 根 pom 的 assembly 只打**源码包**（`source-package.xml`），本分支**不产可运行二进制发布包**，
> 因此要用脚本启动需手动铺 build 布局（见下）。

### 铺 build 布局（供根 `bin` 脚本使用）

```bash
export JAVA_HOME="/c/Program Files/Java/jdk1.8.0_503"
cd /d/workspaces/opensource/zookeeper

# 1) 收集 server 运行期依赖（含 zookeeper-jute jar）到 build/lib
mvn -pl zookeeper-server dependency:copy-dependencies -DincludeScope=runtime \
  -DoutputDirectory="D:/workspaces/opensource/zookeeper/build/lib"

# 2) server 编译产物放到 build/classes
mkdir -p build/classes && cp -r zookeeper-server/target/classes/* build/classes/
```

之后即可用 `bin\zkServer.cmd` / `bin\zkCli.cmd` 启动（PowerShell 里先设好 `JAVA_HOME`）。

### Ant（老构建，可选）

```bash
ant            # 编译，产物在 build/classes + build/lib
ant jar        # 打 zookeeper-<version>.jar
ant package    # 打完整发布包（含 bin/lib/conf）
ant clean
```

> Ant 走 `build.xml` + `ivy.xml` 拉依赖，产物天然是 `build\classes`/`build\lib` 布局，根脚本可直接用。

---

## 四、最快上手（Windows / cmd）

在 cmd 里进入项目目录，然后：

```bat
build-local.cmd     :: 1) 构建（fork JDK8 javac）+ 铺 build\classes、build\lib
run-server.cmd      :: 2) 启动服务端（本窗口前台运行，监听 2181）
```

另开一个 cmd 窗口验证客户端：

```bat
run-cli.cmd ls /
```

**停止服务端**：在服务端窗口 `Ctrl+C`；或 `netstat -ano | findstr :2181` 查 PID 后 `taskkill /F /PID <pid>`。

---

## 附：关键文件清单

| 文件                                                                       | 说明                               |
|--------------------------------------------------------------------------|----------------------------------|
| `conf\zoo.cfg`                                                           | 单机配置，`dataDir=D:/data/zookeeper` |
| `.idea\runConfigurations\ZK_Server__QuorumPeerMain_.xml`                 | IDE 服务端启动项（绑 JDK8）               |
| `.idea\runConfigurations\ZK_Server_Standalone__ZooKeeperServerMain_.xml` | IDE 纯单机启动项（绑 JDK8）               |
| `.idea\runConfigurations\ZK_Client__zkCli_.xml`                          | IDE 客户端启动项（绑 JDK8）               |
| `.idea\misc.xml`                                                         | 项目 SDK = `1.8`                   |
| `build\classes` / `build\lib`                                            | 供根 `bin` 脚本的开发者模式 classpath      |
